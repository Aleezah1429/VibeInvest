[pytest]
# Run from the backend/ directory so `app` (the FastAPI package) is importable,
# matching how Procfile/run.sh launch uvicorn (`uvicorn app.main:app`).
testpaths = tests
python_files = test_*.py
addopts = -q
